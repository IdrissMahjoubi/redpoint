{% extends "base.html.twig" %}
{% block body %}

    <section>
        <div class="container">
            <div class="row">
                {% include('@Frontend/Default/sidebar.html.twig') %}

                <div class="col-sm-9 padding-right">
                    <div class="product-details"><!--product-details-->
                        <div class="col-sm-5">
                            {#<div class="view-product">#}
                                {#<img src="{{ asset('assets/images/product-details/1.jpg')}}" alt="" />#}
                            {#</div>#}
                            <div id="similar-product" class="carousel slide view-product" data-ride="carousel">

                                <!-- Wrapper for slides -->
                                <div class="carousel-inner">

                                    {% for image in product.images %}
                                    <div class="item {% if loop.first %}active{% endif %}">
                                        <img src="{{ asset('uploads/'~image.url)}}" alt="">
                                    </div>
                                    {% endfor %}
                                </div>

                                <!-- Controls -->
                                <a class="left item-control" href="#similar-product" data-slide="prev">
                                    <i class="fa fa-angle-left"></i>
                                </a>
                                <a class="right item-control" href="#similar-product" data-slide="next">
                                    <i class="fa fa-angle-right"></i>
                                </a>
                            </div>

                        </div>
                        <div class="col-sm-7">
                            <div class="product-information"><!--/product-information-->
                                اسم المنتج <h2>{{ product.name }}</h2>
                                وصف <h4>{{ product.description }}</h4>
                                فئة <h4>{{ product.categorie }}</h4>
                                الفئة الفرعية <h4>{{ product.subCategory }}</h4>
                                <span>
									<span>{{ product.price }}KWD</span>
								</span>
                                <p><b>اسم المالك</b></p><h4>{{ product.owner.username }}</h4>
                                    <p><b>رقم الهاتف</b></p><h4> 34 34 34 34</h4>
                                        <p><b>البريد الإلكتروني</b></p><h4> {{  product.owner.email}}</h4>
                            </div><!--/product-information-->
                        </div>
                    </div><!--/product-details-->

                </div>
            </div>
        </div>
    </section>

{% endblock %}